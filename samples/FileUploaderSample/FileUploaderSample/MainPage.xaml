<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:FileUploaderSample"
             x:Class="FileUploaderSample.MainPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Upload" Clicked="OnUpload"></ToolbarItem>
    </ContentPage.ToolbarItems>
    <ScrollView>
        <StackLayout Spacing="10" Padding="10">
            <Button x:Name="takePhoto" Text="Take Photo"/>
            <Button x:Name="pickPhoto" Text="Pick Photo"/>
            <Button x:Name="takeVideo" Text="Take Video"/>
            <Button x:Name="pickVideo" Text="Pick Video"/>
            <ProgressBar x:Name="progress" Progress="0.0" IsVisible="False" />
            <Image x:Name="image"/>
           
        </StackLayout>
    </ScrollView>
</ContentPage>